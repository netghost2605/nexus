NEXUS WebUI — SETUP
===================
1. qBittorrent → Tools → Options → Web UI
2. Tick "Use alternative Web UI"
3. Set path to THIS folder (NEXUS-WebUI, NOT the private subfolder)
4. Click Apply → reload localhost:8080

The folder must contain the "private" subfolder — do not move files around.
